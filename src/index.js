import React from 'react';
import ReactDOM from 'react-dom';
import Home from './Home';
import './index.css';

// <Home /> links to HTML page to render components

ReactDOM.render(
  <Home />,
  document.getElementById('root')
);
